services:
  marian-rknn:
    build:
      context: .
      dockerfile: Dockerfile
    command: python scripts/preflight.py --device cpu --model-name Helsinki-NLP/opus-mt-en-fr
    volumes:
      - .:/workspace

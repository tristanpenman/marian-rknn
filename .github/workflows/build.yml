name: Build C++ Binaries

on:
  workflow_dispatch:
  push:
    paths:
      - cpp/**
      - thirdparty/**
      - Dockerfile.android
      - Dockerfile.native
      - .github/workflows/build.yml
  pull_request:
    paths:
      - cpp/**
      - thirdparty/**
      - Dockerfile.android
      - Dockerfile.native
      - .github/workflows/build.yml

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Android container
        run: |
          docker build -t marian-rknn-android -f Dockerfile.android .

      - name: Build Android binary
        run: |
          docker run --rm \
            -v "$PWD":/workspace \
            marian-rknn-android \
            bash -c "scripts/build-android.sh"

      - name: Upload Android marian-rknn binary
        uses: actions/upload-artifact@v4
        with:
          name: marian-rknn-android-arm64-v8a
          path: build-android/marian-rknn
          if-no-files-found: error

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build container
        run: |
          docker build -t marian-rknn-native -f Dockerfile.native .

      - name: Configure and build
        run: |
          docker run --rm \
            -v "$PWD":/workspace \
            marian-rknn-native \
            bash -c "cmake -B build -DCMAKE_BUILD_TYPE=Release && cmake --build build --config Release"

      - name: Upload marian-rknn binary
        uses: actions/upload-artifact@v4
        with:
          name: marian-rknn-linux-aarch64
          path: build/marian-rknn
          if-no-files-found: error

name: Build C++ Binaries

on:
  workflow_dispatch:
  push:
    paths:
      - cpp/**
      - thirdparty/**
      - Dockerfile.android
      - Dockerfile.build
      - .github/workflows/build.yml
  pull_request:
    paths:
      - cpp/**
      - thirdparty/**
      - Dockerfile.android
      - Dockerfile.build
      - .github/workflows/build.yml

jobs:
  build-android:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build Android container
        run: |
          docker build -t marian-rknn-android -f Dockerfile.android .

      - name: Build Android binary
        run: |
          docker run --rm \
            -v "$PWD":/workspace \
            marian-rknn-android \
            bash -c "cpp/scripts/android-build.sh"

      - name: Upload Android marian-rknn binary
        uses: actions/upload-artifact@v4
        with:
          name: marian-rknn-android-arm64-v8a
          path: cpp/build-android/marian-rknn
          if-no-files-found: error

  build-linux:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build container
        run: |
          docker build -t marian-rknn-build -f Dockerfile.build .

      - name: Configure and build
        run: |
          docker run --rm \
            -v "$PWD":/workspace \
            marian-rknn-build \
            bash -c "cmake -S cpp -B cpp/build -DCMAKE_BUILD_TYPE=Release && cmake --build cpp/build --config Release"

      - name: Upload marian-rknn binary
        uses: actions/upload-artifact@v4
        with:
          name: marian-rknn-linux-aarch64
          path: cpp/build/marian-rknn
          if-no-files-found: error
